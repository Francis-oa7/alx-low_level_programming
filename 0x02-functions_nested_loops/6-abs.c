#include "main.h"
/**
 * _abs - prints the absolute value of number
 * Return: absolute number of the input digit
 * @c: number to absolute
 */
int _abs(int c)
{
	if (c >= 0)
		return (c);
	else
		return (c * -1);
}
